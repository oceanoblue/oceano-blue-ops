# Fly.io config for the Oceano ARW conversion worker.
# Deploy:
#   cd worker
#   fly launch --copy-config --no-deploy   # first time only
#   fly secrets set SUPABASE_URL=https://...supabase.co \
#                   SUPABASE_SERVICE_KEY=eyJ... \
#                   WORKER_SECRET=$(openssl rand -hex 32)
#   fly deploy

app = "oceano-arw-worker"
primary_region = "iad"   # change to your nearest Fly region

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "suspend"     # save money when idle
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  # A 24 MP ARW (50–60 MB compressed) expands to a 16-bit linear TIFF of
  # 250–400 MB during decode, plus Sharp working buffers + Node.js overhead.
  # 512 MB OOM-kills the process; 2 GB has comfortable headroom for one
  # concurrent convert.
  memory_mb = 2048

[deploy]
  release_command = "node -v"

[env]
  NODE_ENV = "production"
